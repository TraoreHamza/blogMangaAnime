{% extends 'base.html.twig' %}

{% block body %}
<h1>Test Upload ImageService</h1>

{{ form_start(form) }}
    {{ form_row(form.imageFile) }}
    <button class="btn btn-primary">Envoyer</button>
{{ form_end(form) }}

{% if fichierUpload %}
    <h2>Résultat :</h2>
    <p>Nom du fichier : {{ fichierUpload }}</p>
    <img src="{{ asset('medias/images/' ~ fichierUpload) }}" alt="Image">
{% endif %}

{% endblock %}
